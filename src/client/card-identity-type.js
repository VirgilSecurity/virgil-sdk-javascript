'use strict';

/**
 * Enumeration of identity types supported by Global Cards.
 * @readonly
 * @enum {string}
 * */
var IdentityType = {
	APPLICATION: 'application',
	EMAIL: 'email'
};

module.exports = IdentityType;
