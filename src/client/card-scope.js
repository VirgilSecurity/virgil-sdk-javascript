'use strict';

/**
 * Enumeration of supported Virgil Card scopes.
 * @readonly
 * @enum {string}
 * */
var CardScope = {
	APPLICATION: 'application',
	GLOBAL: 'global'
};

module.exports = CardScope;
